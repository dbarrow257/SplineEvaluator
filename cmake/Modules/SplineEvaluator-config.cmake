SET(SPLINEEVALUATOR_LIB_LIST "-libOscProbCalcer -libOscillator")

SET(SPLINEEVALUATOR_FEATURES_LIST)

if(UseGPU EQUAL 1)
  LIST(APPEND SPLINEEVALUATOR_FEATURES_LIST "GPU")
endif()
LIST(APPEND SPLINEEVALUATOR_FEATURES_LIST ${NuOscillator_Engines_Enabled})

SET(ROOT_INCLUDE_DIRS_SEP ${ROOT_INCLUDE_DIRS})
string(REPLACE ";" " -I" ROOT_INCLUDE_DIRS_SEP "-I${ROOT_INCLUDE_DIRS_SEP}")
SET(ROOT_DEFINITIONS_SEP ${ROOT_DEFINITIONS})
string(REPLACE ";" " " ROOT_DEFINITIONS_SEP "${ROOT_DEFINITIONS_SEP}")
SET(ROOT_LIBRARIES_SEP ${ROOT_LIBRARIES})
string(REPLACE ";" " -l" ROOT_LIBRARIES_SEP "-l${ROOT_LIBRARIES_SEP}")

# Set the creation date
string(TIMESTAMP CREATION_DATE "%d-%m-%Y")

string(REPLACE ";" " " SPLINEEVALUATOR_FEATURES "${SPLINEEVALUATOR_FEATURES_LIST}")
configure_file(${CMAKE_CURRENT_LIST_DIR}/../Templates/SplineEvaluator-config.in
  "${PROJECT_BINARY_DIR}/SplineEvaluator-config" @ONLY)
install(PROGRAMS
  "${PROJECT_BINARY_DIR}/SplineEvaluator-config" DESTINATION
  bin)
